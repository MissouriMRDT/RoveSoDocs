name: Build & Deploy Docs Hub

on:
  push:
    branches: [ "development" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  # -------------------------
  # 1) Build VitePress hub (root)
  # -------------------------
  build_hub:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout docs hub
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Node deps
        run: npm ci

      - name: Build VitePress into out/
        run: |
          set -euo pipefail
          npm run docs:build
          mkdir -p out
          rsync -a .vitepress/dist/ out/

      - name: Upload hub artifact
        uses: actions/upload-artifact@v4
        with:
          name: hub-out
          path: out

  # -------------------------
  # 2) Build RoveSoSimulator (Jekyll)
  # -------------------------
  build_rovesosim_jekyll:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout RoveSoSimulator (Jekyll)
        uses: actions/checkout@v4
        with:
          repository: MissouriMRDT/RoveSoSimulator
          ref: github-pages
          path: src/RoveSoSimulator
          fetch-depth: 1
          token: ${{ secrets.DOCS_BOT_TOKEN || github.token }}

      - name: Setup Ruby (for Jekyll) - no cache
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2.4"
          bundler-cache: false
          working-directory: src/RoveSoSimulator

      - name: Install gems (fresh, no cache)
        working-directory: src/RoveSoSimulator
        env:
          BUNDLE_WITHOUT: "development:test"
        run: |
          set -euo pipefail
          rm -rf .bundle vendor/bundle Gemfile.lock
          gem --version
          bundler --version || gem install bundler
          bundle config set --local path 'vendor/bundle'
          bundle config set --local clean 'true'
          bundle config set --local without 'development:test'
          bundle install --jobs 4 --retry 3

      - name: Build RoveSoSimulator site into out/
        working-directory: src/RoveSoSimulator
        env:
          JEKYLL_ENV: production
          PAGES_REPO_NWO: MissouriMRDT/RoveSoSimulator
          JEKYLL_GITHUB_TOKEN: ${{ secrets.DOCS_BOT_TOKEN || github.token }}
        run: |
          set -euo pipefail
          mkdir -p "$GITHUB_WORKSPACE/out/RoveSoSimulator/_j"
          bundle exec jekyll build \
            --baseurl "/RoveSoSimulator/_j" \
            -d "$GITHUB_WORKSPACE/out/RoveSoSimulator/_j"

      - name: Upload simulator jekyll artifact
        uses: actions/upload-artifact@v4
        with:
          name: sim-jekyll-out
          path: out

  # -------------------------
  # 3) Stage RoveSoSimulator (Doxygen) from .static in hub repo
  # -------------------------
  build_rovesosim_doxygen:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout docs hub (for .static/)
        uses: actions/checkout@v4

      - name: Stage RoveSoSimulator Doxygen into out/
        run: |
          set -euo pipefail
          mkdir -p out/RoveSoSimulator/_d

          if [ -d ".static/RoveSoSimulatorDoxygen/html" ]; then
            rsync -a ".static/RoveSoSimulatorDoxygen/html/" "out/RoveSoSimulator/_d/"
          elif [ -d ".static/RoveSoSimulatorDoxygen" ]; then
            rsync -a ".static/RoveSoSimulatorDoxygen/" "out/RoveSoSimulator/_d/"
          else
            echo "ERROR: Could not find Doxygen HTML output folder."
            exit 1
          fi

      - name: Upload simulator doxygen artifact
        uses: actions/upload-artifact@v4
        with:
          name: sim-doxygen-out
          path: out

  # -------------------------
  # 4) Build Autonomy_Software (Doxygen)
  # -------------------------
  build_autonomy_doxygen:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Autonomy_Software (Doxygen)
        uses: actions/checkout@v4
        with:
          repository: MissouriMRDT/Autonomy_Software
          path: src/Autonomy_Software
          fetch-depth: 1
          submodules: false
          token: ${{ secrets.DOCS_BOT_TOKEN || github.token }}

      - name: Init only needed submodules (and block LiDAR)
        working-directory: src/Autonomy_Software
        shell: bash
        run: |
          set -euo pipefail

          # Only allow these submodules to ever be touched
          git config --local --unset-all submodule.active || true
          git config --local --add submodule.active external/threadpool
          git config --local --add submodule.active external/rovecomm

          # Extra safety: make LiDAR a no-op if anything tries to update all submodules
          git config --local submodule.data/LiDAR.update none

          git submodule sync --recursive
          git submodule update --init --recursive external/threadpool external/rovecomm

      - name: Install Doxygen + Graphviz + rsync
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen graphviz rsync

      - name: Run Doxygen
        working-directory: src/Autonomy_Software
        run: doxygen ./Doxyfile

      - name: Stage Autonomy docs into out/
        run: |
          set -euo pipefail
          mkdir -p out/autonomy

          if [ -d "src/Autonomy_Software/docs/Doxygen/html" ]; then
            rsync -a "src/Autonomy_Software/docs/Doxygen/html/" "out/autonomy/"
          elif [ -d "src/Autonomy_Software/docs/Doxygen" ]; then
            rsync -a "src/Autonomy_Software/docs/Doxygen/" "out/autonomy/"
          elif [ -d "src/Autonomy_Software/html" ]; then
            rsync -a "src/Autonomy_Software/html/" "out/autonomy/"
          else
            echo "ERROR: Could not find Doxygen HTML output folder."
            exit 1
          fi

      - name: Upload autonomy doxygen artifact
        uses: actions/upload-artifact@v4
        with:
          name: autonomy-out
          path: out

  # -------------------------
  # 5) Build Embedded_Docs (Doxygen)
  # -------------------------
  build_embedded_doxygen:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Embedded_Docs (Doxygen)
        uses: actions/checkout@v4
        with:
          repository: MissouriMRDT/Embedded_Docs
          path: src/Embedded_Docs
          fetch-depth: 1
          submodules: recursive
          token: ${{ secrets.DOCS_BOT_TOKEN || github.token }}

      - name: Install Doxygen + Graphviz + rsync
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen graphviz rsync

      - name: Run Doxygen
        working-directory: src/Embedded_Docs/docs/Doxygen
        run: doxygen ./Doxyfile

      - name: Build Sphinx
        run: |
          cd src/Embedded_Docs/docs/Sphinx
          python -m venv .
          source bin/activate
          pip install -r requirements.txt
          make html

      - name: Stage Embedded docs into out/
        run: |
          set -euo pipefail
          mkdir -p out/embedded

          if [ -d "src/Embedded_Docs/docs/Sphinx/build/html" ]; then
            rsync -a "src/Embedded_Docs/docs/Sphinx/build/html/" "out/embedded/"
          else
            echo "ERROR: Could not find Doxygen HTML output folder."
            exit 1
          fi

      - name: Upload embedded doxygen artifact
        uses: actions/upload-artifact@v4
        with:
          name: embedded-out
          path: out

  # -------------------------
  # 6) Build RoveComm C++ (Doxygen)
  # -------------------------
  build_rovecommcpp_doxygen:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout RoveComm C++ (Doxygen)
        uses: actions/checkout@v4
        with:
          repository: MissouriMRDT/RoveComm_CPP
          path: src/RoveComm_CPP
          fetch-depth: 1
          submodules: recursive
          token: ${{ secrets.DOCS_BOT_TOKEN || github.token }}

      - name: Install Doxygen + Graphviz + rsync
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen graphviz rsync

      - name: Run Doxygen
        working-directory: src/RoveComm_CPP
        run: doxygen ./Doxyfile

      - name: Stage RoveComm_CPP docs into out/
        run: |
          set -euo pipefail
          mkdir -p out/rovecomm/_cpp

          if [ -d "src/RoveComm_CPP/docs/Doxygen/html" ]; then
            rsync -a "src/RoveComm_CPP/docs/Doxygen/html/" "out/rovecomm/_cpp/"
          elif [ -d "src/RoveComm_CPP/docs/Doxygen" ]; then
            rsync -a "src/RoveComm_CPP/docs/Doxygen/" "out/rovecomm/_cpp/"
          elif [ -d "src/RoveComm_CPP/html" ]; then
            rsync -a "src/RoveComm_CPP/html/" "out/rovecomm/_cpp/"
          else
            echo "ERROR: Could not find Doxygen HTML output folder."
            exit 1
          fi

      - name: Upload RoveComm_CPP doxygen artifact
        uses: actions/upload-artifact@v4
        with:
          name: rc-cpp-out
          path: out

  # -------------------------
  # 6) Assemble + Search + 404 patch + Publish artifact
  # -------------------------
  assemble:
    runs-on: ubuntu-latest
    needs:
      - build_hub
      - build_rovesosim_jekyll
      - build_rovesosim_doxygen
      - build_autonomy_doxygen
      - build_embedded_doxygen
      - build_rovecommcpp_doxygen

    steps:
      - name: Checkout docs hub
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Node deps
        run: npm ci

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: parts

      - name: Merge artifacts into dist/
        run: |
          set -euo pipefail
          mkdir -p dist

          # Base first (hub), then overlay sub-sites
          rsync -a "parts/hub-out/" dist/
          rsync -a "parts/sim-jekyll-out/" dist/
          rsync -a "parts/sim-doxygen-out/" dist/
          rsync -a "parts/autonomy-out/" dist/
          rsync -a "parts/embedded-out/" dist/
          rsync -a "parts/rc-cpp-out/" dist/

          # GitHub Pages: disable Jekyll processing
          touch dist/.nojekyll

      # - name: Build unified MiniSearch index for dist/
      #   run: node tools/build-minisearch-index.mjs

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: assemble
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
